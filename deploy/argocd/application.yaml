apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arogyagrid-gcp
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/shivam2003-dev/pulsegrid-health-ops.git
    targetRevision: main
    path: deploy/k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: arogyagrid
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
